device: cuda
agent: "DQN"
maze_file: "mazes/10x10.txt"

reward:
  mode: "goal_and_step"  # Reward function: goal_reward at goal, otherwise step_penalty each step.
  goal_reward: 1.0  # Reward returned when agent reaches goal (done=True).
  step_penalty: -0.01  # Per-step penalty used only in goal_and_step mode.

hyperparameters:
  learning_rate: 0.001
  discount_rate: 0.99
  hidden_size: 256
  q_init_strategy: "random_negative"
  q_init_random_low: -0.5
  q_init_random_high: 0.0
  epsilon_policy: "decay"
  epsilon: 1.0
  epsilon_min: 0.05
  epsilon_decay: 0.995
  batch_size: 512
  target_update: 10

training:
  episodes: 500
  max_steps: 200
  render: False
  render_heatmap: True

evaluation:
  enabled: True
  num_videos: 5
  episodes: 1
  max_steps: 50
  save_video: True
  video_heatmap: True
  display: True
  fps: 8

viz:
  num_plots: 4
  window_size: null
